# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: tabs_iframes_dev
  username: root
  password:
  host: localhost
  encoding: utf8
  collation: utf8_general_ci
    

test:
  adapter: mysql
  database: tabs_iframes_test
  username: root
  password: 
  host: localhost
  encoding: utf8
  collation: utf8_general_ci


production:
  adapter: sqlite3
  database: db/production.sqlite3
  pool: 5
  timeout: 5000
